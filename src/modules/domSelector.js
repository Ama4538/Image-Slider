const imageTrack = document.querySelector(".image-container");
const body = document.querySelector("body");
const images = document.querySelectorAll(".image-container__image");

export {
    imageTrack,
    body,
    images,
}